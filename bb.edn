{:paths ["src"]
 :tasks
 {:requires ([ase-scripting.tasks :as tasks]
             [babashka.fs :as fs]
             [clojure.string :as string])

  :init (println "bb.edn running task")

  watch tasks/watch

  regenerate-steam-boxart
  {:description "Regenerate and reexport steam boxart"
   :task
   (do
     (tasks/generate-boxart-files)
     (tasks/export-all-boxart))}}}
